version: '3.3'
services:
  fastapi:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    restart: always
    volumes:
      - "./docker/api:/home/app_ping/"
    environment:
      - DEV_ENV=True
      - TEST_ENV=True
